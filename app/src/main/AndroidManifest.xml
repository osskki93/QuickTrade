<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.oscar.app1pm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity_Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity_Registro" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ModifcarDatos" />
        <activity android:name=".AnyadirProducto" />
        <activity android:name=".AdaptadorRecyclerProductos" />
        <activity android:name=".DetalleProductoActivity" />
        <activity android:name=".FavoritosActivity" />
        <activity android:name=".AdaptadorRecyclerUsuarios" />
        <activity android:name=".ListaUsuarios" />
        <activity android:name=".DetalleUsuarioActivity"></activity>
    </application>

</manifest>